[project]
name = "airflow-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MOHAMMED EL MAKHLOUFI", email = "m.mohammedelmakhloufi@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "airflow-operators>=0.11.0",
    "apache-airflow>=3.3.1",
]

[project.scripts]
airflow-tutorial = "airflow_tutorial:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
